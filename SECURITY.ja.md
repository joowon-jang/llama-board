# セキュリティポリシー

> **言語:** [English](SECURITY.md) | [한국어](SECURITY.ko.md) | [日本語](SECURITY.ja.md) | [中文](SECURITY.zh.md)

## 報告

公開 issue を作成しないでください。[GitHub Security Advisories](https://github.com/joowon-jang/llama-board/security/advisories/new) を使用するか、メンテナーに非公開で連絡してください。最小の再現を含め、シークレットは `[REDACTED]` に置き換えてください。

## 対象範囲

ランタイムアーカイブ検証、パストラバーサル、PR ソースビルドの来歴、プロセスライフサイクル、ローカルエンドポイントの公開、インストーラー整合性、Tauri capabilities。

## ローカル認証

デスクトップ管理サーバーは `127.0.0.1` にバインドされ、起動ごとに一時的な `--api-key-file` を通じてベアラートークンを渡します。一時ファイルは起動後に削除され、トークンは停止時にクリアされます。同じ Windows ユーザー権限のプロセスはサーバーを検査または操作できる場合がありますが、これは想定内です。エンドポイントやキーを共有しないでください。

ヘッドレス CLI の `server start` モードは異なります。このモードは意図的に `--no-api-key` を使用し、`127.0.0.1` バインドに依存します。そのポートにアクセスできるローカルプロセスは呼び出せるため、信頼できる PC だけで使用し、ポートを外部公開・転送しないでください。

開発者ゲートウェイもローカル専用です。MCP サーバーはユーザー権限で起動する第三者の実行ファイルなので、信頼できる実行ファイルだけを追加し、設定した承認ポリシーに従ってツール呼び出しを確認してください。

## 事前ビルド済み PR アーティファクト

ポータブル ZIP とワークフローで公開された CPU アーティファクトは、platform/arch/backend/build-id、GitHub アセット SHA-256、ファイル単位マニフェスト SHA-256、埋め込み PR リポジトリ/コミット、必須実行ファイル、正常なプレフライトを検証します。受信 PC は CMake/コンパイラ/SDK 不要ですが、GPU ランタイムは互換 GPU ドライバーが必要です。ワークフローは既定で CPU のみを公開し、GPU は SDK 搭載マシンからエクスポートする必要があります。

## ソースからの PR ビルド

第三者のコードをユーザー権限でコンパイルします。ダウンロード前に来歴（タイトル/作成者/リポジトリ/ブランチ/コミット/状態）を表示し、そのコミットに固定 — head が移動した場合は拒否します。PR ソースアーカイブはコミット固定 HTTPS で取得し、期待されるコミットディレクトリを検査します。GitHub はそのソースアーカイブの信頼できるダイジェストを公開しないため、保存された SHA-256 はローカル監査記録です。リリースインストーラーは可能な場合 GitHub アセットのダイジェストを使用し、なければ `checksums.txt` を代替として使用します。ビルドはアーカイブ内に限定（BoringSSL/libcurl/OpenSSL off）、2段階確認が必要、すべての PR 状態が明示されます。対応バックエンド: `cpu` `vulkan` `cuda` `rocm`（SYCL/OpenVINO は拒否）。

## 子プロセス環境

`llama-server`、`llama-bench`、CMake ビルドはクリアされた env + プラットフォーム別の明示的許可リストで実行されます。プロキシ/TLS 変数は企業ビルドのために渡され、シークレット（`GITHUB_TOKEN`、`*_TOKEN`、`*_PASSWORD`、`SSH_AUTH_SOCK` など）は除外されテストされます。Git 認証情報ヘルパーは無効化されます。

## サポートバージョン

最新リリースと `main` が修正対象です。古いリリースはアップグレードが必要な場合があります。
