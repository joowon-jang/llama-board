# 보안 정책

> **언어:** [English](SECURITY.md) | [한국어](SECURITY.ko.md) | [日本語](SECURITY.ja.md) | [中文](SECURITY.zh.md)

## 제보

공개 이슈를 열지 마세요. [GitHub Security Advisories](https://github.com/joowon-jang/llama-board/security/advisories/new)를 이용하거나 메인테이너에게 비공개로 연락하세요. 최소 재현을 포함하고 시크릿은 `[REDACTED]`로 대체하세요.

## 범위

런타임 아카이브 검증, 경로 탐색, PR 소스 빌드 출처, 프로세스 라이프사이클, 로컬 엔드포인트 노출, 인스톨러 무결성, Tauri capabilities.

## 로컬 인증

데스크톱이 관리하는 서버는 `127.0.0.1`에 바인딩되며 시작마다 임시 `--api-key-file`을 통해 베어러 토큰을 전달합니다. 임시 파일은 시작 후 삭제되고 토큰은 중지 시 지워집니다. 동일 Windows 사용자 권한으로 실행되는 프로세스는 여전히 서버를 조회하거나 사용할 수 있으며 이는 정상입니다. 엔드포인트나 키를 공유하지 마세요.

헤드리스 CLI의 `server start` 모드는 다릅니다. 이 모드는 의도적으로 `--no-api-key`를 사용하고 `127.0.0.1` 바인딩에 의존합니다. 해당 포트에 접근할 수 있는 로컬 프로세스는 호출할 수 있으므로 신뢰할 수 있는 컴퓨터에서만 사용하고 포트를 외부에 노출하거나 전달하지 마세요.

개발자 게이트웨이도 로컬 전용입니다. MCP 서버는 사용자 권한으로 실행되는 제3자 실행 파일이므로 신뢰하는 실행 파일만 추가하고 설정한 승인 정책에 따라 도구 호출을 검토하세요.

## 사전 빌드된 PR 아티팩트

휴대용 ZIP과 워크플로에서 게시된 CPU 아티팩트는 플랫폼/arch/백엔드/build-id, GitHub 에셋 SHA-256, 파일 단위 매니페스트 SHA-256, 포함된 PR 저장소/커밋, 필수 실행 파일, 정상 프리플라이트를 검증합니다. 받는 PC는 CMake/컴파일러/SDK가 필요 없지만 GPU 런타임은 호환 GPU 드라이버가 필요합니다. 워크플로는 기본 CPU만 게시하며 GPU는 SDK가 있는 머신에서 내보내야 합니다.

## 소스에서 PR 빌드

제3자 코드를 사용자 권한으로 컴파일합니다. 다운로드 전 출처(제목/작성자/저장소/브랜치/커밋/상태)를 표시하고 해당 커밋에 고정 — 헤드가 이동하면 거부합니다. PR 소스 아카이브는 커밋 고정 HTTPS로 가져오고 예상 커밋 디렉터리를 검사합니다. GitHub는 해당 소스 아카이브의 신뢰할 수 있는 다이제스트를 게시하지 않으므로 저장된 SHA-256은 로컬 감사 기록입니다. 릴리스 인스톨러는 가능한 경우 GitHub 에셋 다이제스트를 사용하고, 없으면 `checksums.txt`를 대체 수단으로 사용합니다. 빌드는 아카이브 내부로 제한(BoringSSL/libcurl/OpenSSL off), 2단계 확인 필요, 모든 PR 상태가 명시됩니다. 지원 백엔드: `cpu` `vulkan` `cuda` `rocm` (SYCL/OpenVINO 거부).

## 자식 프로세스 환경

`llama-server`, `llama-bench`, CMake 빌드는 비워진 env + 플랫폼별 명시적 허용 목록으로 실행됩니다. 프록시/TLS 변수는 기업 빌드를 위해 전달되며, 시크릿(`GITHUB_TOKEN`, `*_TOKEN`, `*_PASSWORD`, `SSH_AUTH_SOCK` 등)은 제외되고 테스트됩니다. Git 자격 증명 헬퍼는 비활성화됩니다.

## 지원 버전

최신 릴리스와 `main`이 수정 대상입니다. 이전 릴리스는 업그레이드가 필요할 수 있습니다.
